{{!--add person - complete--}}

<h2>Add Person</h2>
<form action="/add/person/" method="POST">
	<div class="form-group">
		<label for="villain">Is Villain?</label>
		<input type="checkbox" class="form-control" id="villain" name="villain">
	</div>
	<div class="form-group">
		<label for="name">Alias</label>
		<input type="text" class="form-control" id="name" name="name" placeholder="Alias">
		<p style="display:none;" id="name_error"><strong style="color:red;">You must provide an name.</strong></p>
	</div>
	<div class="form-group">
		<label for="real_name">Real Name</label>
		<input type="text" class="form-control" id="real_name" name="real_name" placeholder="Real Name">
	</div>
	{{!--<div class="form-group">--}}
	{{!--	<label for="superpower">Power</label>--}}
	{{!--	<select class="form-control" id="superpower" name="superpower">--}}
	{{!--		{{#each form_data.superpower}}--}}
	{{!--			<option value="{{id}}">{{spow}}</option>--}}
	{{!--		{{/each}}--}}
	{{!--	</select>--}}
	{{!--</div>--}}
	{{!--<div class="form-group">--}}
	{{!--	<label for="city">City</label>--}}
	{{!--	<select class="form-control" id="city" name="city">--}}
	{{!--		{{#each form_data.city}}--}}
	{{!--			<option value="{{id}}">{{name}}</option>--}}
	{{!--		{{/each}}--}}
	{{!--	</select>--}}
	{{!--</div>--}}
	<div class="form-group">
		<label for="origin">Origin Story</label>
		<input type="text" class="form-control" id="origin" name="origin" placeholder="Origin Story">
	</div>
	<button type="submit" class="btn btn-default">Add Person</button>
</form>
<script src="/js/person_form.js" type="text/javascript"></script>
